function printf(fmt: *i8, ...) -> i32;

function add(x: i32, y: i32) -> i32 {
  let z = x + y;
  printf("%d + %d = %d\n", x, y, z);
  return z;
}

function main() -> i32 {
  // check: 1 + 2 = 3
  let f: *function(i32, i32) -> i32 = &add;
  f(1, 2);

  return 0;
}
