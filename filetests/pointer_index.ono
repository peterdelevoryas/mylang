import libc;

function main() -> i32 {
  // check: 0123456789
  let x = libc.malloc(10);
  for let i = 0; i < 10; i += 1 {
    x[i] = i as i8;
  }
  for let i = 0; i < 10; i += 1 {
    libc.printf("%d", x[i]);
  }
  libc.printf("\n");
  libc.free(x);
  return 0;
}
